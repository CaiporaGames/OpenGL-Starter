Future looks:
- callback vs polling:
	For continuous controls (movement, holding buttons) → polling is fine.
	For instant events (keypress, text input, resize, clicks) → callbacks are safer.
	Best practice: use callbacks to fill an input state, then let your game logic read that state each frame.